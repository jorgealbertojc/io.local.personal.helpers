# 1.0.1

Second release of **oswrap**. Adds a one-line installer, shell
completion for the `os` command, and documentation updates. No changes
to the existing behavior of `os update`.

## Overview

This release focuses on installation ergonomics and shell integration.
The previous version required the user to know about `uv tool install`
and to run a separate command to activate completion; both are now
handled automatically by `install.sh`.

No command logic changed. `os update` behaves exactly as it did in
`1.0.0`.

## Requirements

Unchanged from `1.0.0`:

- Ubuntu 24.04 LTS (or any Debian-based distribution with `apt`).
- Python 3.12 or newer.
- `uv` as the dependency manager and tool runner.
- `curl` and `git` for the one-line installer.
- The current user must be `root` or a member of the `sudo` group to
  run privileged commands.

## Installation

### New in this release: one-line installer

```bash
curl -fsSL https://gitlab.com/jorgealbertojc/io.local.personal.helpers/-/raw/master/install.sh | sh
```

The installer:

1. Verifies that `curl` and `git` are available.
2. Installs `uv` from the official installer if it is not on `PATH`.
3. Installs the package as a `uv` tool, pinned to release `1.0.1`.
4. Activates `argcomplete`'s global shell completion.
5. Prints the location of the `os` command and how to verify it.

The script is POSIX `sh` compatible, so it can be safely piped into a
shell. It is idempotent: re-running it upgrades or reinstalls the tool
without manual cleanup.

#### Installer environment variables

| Variable | Default | Description |
|----------|---------|-------------|
| `OSWRAP_VERSION` | `1.0.1` | Git tag to install. |
| `OSWRAP_REPO` | GitLab SSH URL | Repository to install from. Override to install from the GitHub mirror, a fork, or a local path. |

### Manual installation with `uv tool install`

Unchanged from `1.0.0`. See the README for the full reference.

```bash
uv tool install git+ssh://git@gitlab.com/jorgealbertojc/io.local.personal.helpers.git@1.0.1
```

## Shell completion

New in this release. Tab completion is available for commands,
aliases, and flags, once `argcomplete`'s global hook is active.

If you installed through `install.sh`, completion is already active.
If you installed manually, activate the global hook once per machine:

```bash
uvx --from argcomplete activate-global-python-argcomplete --user
exec $SHELL
```

Verify:

```bash
os <TAB>          # shows: update  apt-update  help
os update <TAB>   # shows: -v  --verbose  --help  -h
```

Under the hood:

- `argcomplete` is now a runtime dependency.
- `cli.py` exposes `_build_completion_parser`, an argparse parser
  used exclusively for completion. It does not participate in
  argument dispatch; it exists only so `argcomplete` can enumerate
  the registered commands, aliases, and global flags.
- `cli.py` carries the `# PYTHON_ARGCOMPLETE_OK` marker on its first
  line. `argcomplete`'s global hook scans for that marker to decide
  which commands to complete, so per-command registration is not
  required.

## Commands

### `os update`

Unchanged from `1.0.0`. See `releases/1.0.0.md` for the full
reference of the workflow.

## Architecture

The only new file at the repository root is `install.sh`:

```
io.local.personal.helpers/
├── install.sh                   # one-line installer (POSIX sh)
├── pyproject.toml
├── uv.lock
├── README.md
├── releases/
│   ├── 1.0.0.md
│   └── 1.0.1.md
└── src/
    └── oswrap/
        ├── __init__.py          # package metadata (__version__)
        ├── cli.py               # entry point: parsing, dispatch, completion
        ├── helptext.py          # general help presentation
        ├── commands/
        │   ├── __init__.py      # command registry
        │   └── apt.py           # implementation of "os update"
        └── lib/
            ├── __init__.py
            ├── logs.py          # ExecutionLog context manager
            └── sudo.py          # sudo helpers
```

## Design Decisions

### Shell completion via `argcomplete`

A dedicated argparse parser in `cli.py` is used exclusively for
completion. It never participates in argument dispatch. Keeping it
separate avoids forcing a full argparse migration before the CLI has
enough commands to justify one.

Global activation via `activate-global-python-argcomplete` means per-
command registration is not required. The entry point only needs the
`# PYTHON_ARGCOMPLETE_OK` marker on its first line.

The marker is placed on **line 1**, not merely within the first ten
lines, so that future edits to the file header cannot drift it out of
the range `argcomplete` scans.

### One-line installer written in POSIX `sh`

When the script is piped into a shell, the shebang is ignored and the
interpreter used is `dash` on Ubuntu. Bash-only constructs would fail
silently in that path, so the script sticks to POSIX syntax on
purpose. No `pipefail`, no `[[ ]]`, no arrays.

### Installer default version

`install.sh` pins a default version via `OSWRAP_VERSION` (currently
`1.0.1`). This means the installer served from `master` always installs
the version declared in its own default, not the latest tag. Bumping
the default is part of the release process for now. A future release
may derive the version dynamically from the latest tag via the GitLab
or GitHub API, at the cost of an extra network round-trip.

## Known Limitations

- Only one command is available: `os update`.
- The parsing of apt's summary line depends on the English output
  format. A future release will force `LC_ALL=C` for the apt
  subprocesses.
- No validation of unknown flags. `os update --frobnicate` silently
  ignores the unknown flag. A formal argument parser will be
  introduced alongside the second command.
- Ubuntu version is not validated at runtime; the help text mentions
  24.04 LTS purely as documentation.
- The install script does not verify a checksum or signature of the
  downloaded repository content. This will be addressed once release
  artifacts are hashed in the release notes.

## Repository

- **Primary (GitLab):** https://gitlab.com/jorgealbertojc/io.local.personal.helpers
- **Mirror (GitHub):** https://github.com/jorgealbertojc/io.local.personal.helpers
